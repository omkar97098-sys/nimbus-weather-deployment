# OpenWeatherMap API key — get one at https://openweathermap.org/api
OPENWEATHER_API_KEY=your_openweather_api_key_here

# MongoDB connection string (optional — falls back to a local JSON file)
# MONGODB_URI=mongodb://127.0.0.1:27017/nimbus

# PORT=5000