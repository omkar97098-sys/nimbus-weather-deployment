# Base URL of the deployed NIMBUS backend (Render web service),
# e.g. https://nimbus-api.onrender.com
#
# When unset, the app calls the same origin (/api/*), which is used
# during local development via the Vite proxy in vite.config.js.
#
# VITE_API_BASE_URL=https://your-backend.onrender.com